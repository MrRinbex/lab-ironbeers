<div>
    {{#each beer}}
    <div class="beersPage">
    <img src="{{image_url}}" alt="image beers">
    <div id="beersPage">
    <h1>{{name}}</h1>
    <h3>{{tagline}}</h3>
    <p>{{description}}</p>
    <p>{{food_pairing}}</p>
    <p>{{brewers_tips}}</p>
    </div>
    </div>
    {{/each}}
</div>